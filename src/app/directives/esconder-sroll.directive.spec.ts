import { EsconderSrollDirective } from './esconder-sroll.directive';

describe('EsconderSrollDirective', () => {
  it('should create an instance', () => {
    const directive = new EsconderSrollDirective();
    expect(directive).toBeTruthy();
  });
});
