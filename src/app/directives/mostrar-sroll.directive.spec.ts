import { MostrarSrollDirective } from './mostrar-sroll.directive';

describe('MostrarSrollDirective', () => {
  it('should create an instance', () => {
    const directive = new MostrarSrollDirective();
    expect(directive).toBeTruthy();
  });
});
